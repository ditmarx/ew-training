// import { SxProps, Theme } from '@mui/system';
import { BoxProps } from '@mui/material';

type ChipProps = {
    gradient?: boolean;
    // sx?: SxProps<Theme>;
} & BoxProps;

export default ChipProps;
